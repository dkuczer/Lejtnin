({
    init: function(component, event, helper) {
        helper.handleInift(component);
    },

    searchByFamily: function (component, event, helper) {
        helper.handleSearchByFamily(component, event);
    }
})